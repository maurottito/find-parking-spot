# Python dependencies for Speed Layer (EMR Cluster)
# Install with: pip3 install --user -r requirements.txt

# Computer Vision & Image Processing
opencv-python-headless>=4.10.0  # Headless version for servers (no GUI dependencies)
numpy>=1.21.0                   # Required by OpenCV for array operations

# HBase Client
happybase>=1.2.0                # Python client for HBase/Thrift connection

# Note: Use opencv-python-headless instead of opencv-python to avoid
# GUI library dependencies (libGL, GTK, etc.) on headless servers
